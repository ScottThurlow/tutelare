# Private working notes — never commit
/notes/

# Environment / secrets
.env
.env.*
!.env.example

# macOS
.DS_Store

# Logs
*.log

# Editor
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example
.idea/

# Claude Code (local user settings only — keep shared config in repo)
.claude/settings.local.json

# Node / Astro (added pre-emptively; populated in next commit)
node_modules/
dist/
.astro/
*.log

# AI oversight protocol
# prompts/ and AGENTS.md are always committed — never ignore them
.ai-local/
.claude/settings.local.json
