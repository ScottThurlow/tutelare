# Private working notes — never commit
/notes/

# Environment / secrets
.env
.env.*
!.env.example

# macOS
.DS_Store

# Logs
*.log

# Editor
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example
.idea/

# Claude Code (local user settings only — keep shared config in repo)
.claude/settings.local.json

# Node / Astro
node_modules/
.astro/
*.log

# dist/ is built by CI and force-added there; do not commit locally.
# CI uses `git add -f dist/` to override this entry; humans should not.
dist/
